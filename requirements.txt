# Resourcehip static site builder dependencies
# Install with: pip install -r requirements.txt
# Cloudflare Pages build command: pip install -r requirements.txt && python build.py

jinja2>=3.1
pyyaml>=6.0
markdown>=3.5
